<% provide(:title, @user.name) %>
<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
</h1>
<h3>料理 (<%= @user.dishes.count %>)</h3>
<section class="stats">
  <%= render 'shared/stats' %>
</section>
<%= render 'follow_form' if logged_in? %>